export interface Cocktail {
  name: string;
  description: string;
  price: string;
  category: string;
}

export interface Feature {
  title: string;
  description: string;
}

export interface ContactForm {
  name: string;
  email: string;
  message: string;
} 